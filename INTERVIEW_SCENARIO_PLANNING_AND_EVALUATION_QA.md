# 医学域自适应 RAG 面试补充：场景、结果分析、Planning 与 Summary 深挖

## 1. 这份文档解决什么问题

这份文档不是再重复一遍“项目做了哪些模块”，而是专门补你在面试里最容易被追问、但最难答深的几个点：

- 这个项目到底服务什么真实场景，而不是一个“能跑通的 toy demo”
- 为什么路由不能只按问题长短分，而要结合风险、复杂度和证据依赖
- 为什么医学场景下不能只看 Recall、NDCG、ROUGE 这些指标
- 结果到底应该怎么分析，怎样看输出是否真的“好”
- 如果用更强的模型来做答案质量分析，应该怎么设计 judge 流程
- 实施前的 planning 到底在系统里承担什么作用，为什么不是可有可无
- 最后的 summary 到底是什么，它为什么不是“把检索结果拼一拼”

建议你在面试时把这份文档理解成四层表达：

1. 先讲场景约束
2. 再讲系统为什么这样设计
3. 再讲你怎么判断它有没有真的变好
4. 最后讲你看过哪些失败案例，以及后来怎么修

如果你能按这个顺序讲，面试官通常会明显感觉到你不是只会背架构图。

---

## 2. 先背住的总定位

你可以先把下面这段练熟，很多问题都可以从这里往下展开：

> 这个项目不是一个通用聊天机器人，也不是一个“先检索再生成”的固定 RAG，而是一个面向医学信息辅助问答场景的自适应 RAG 系统。  
> 它的核心目标不是单纯把 benchmark 指标做高，而是让系统知道：什么时候可以低成本直答，什么时候必须检索，什么时候必须进入多轮补证和跨源验证。  
> 在医学场景里，最怕的不是答不出来，而是答得很像对的，但证据不够、风险意识不够或者把不确定问题说得过于确定。  
> 所以我把系统重点放在三件事上：动态路由、证据约束、结果评估闭环。

如果面试官追问“你这个系统的边界是什么”，建议你接这一句：

> 它的定位是医学信息辅助与证据增强问答，不是替代医生做最终诊疗决策的自动诊断系统。高风险问题至少强制进入检索链路，答案也要求显式表达证据边界和不确定性。

---

## 3. 场景理解：为什么这个项目不是 toy

### 3.1 真实场景的核心矛盾

这个系统面向的是医学信息问答，而不是普通百科问答。医学场景天然有四个约束：

- 用户 query 很脏。会出现缩写、口语、错别字、症状替代标准诊断、多个意图混在一起、中文英文混用等情况。
- 问题风险不均匀。有些只是术语解释，有些却是药物联用、禁忌、特殊人群、综合治疗策略，这类问题即使句子很短，风险也很高。
- 正确答案不能只看“像不像”。必须看答案有没有证据支持，证据是否覆盖关键条件，是否把不确定说清楚。
- 不是所有问题都值得走最重的 agent 链路。真实系统必须在准确性、时延、成本之间做平衡。

你可以把这四点理解成系统设计的来源：

- 因为 query 脏，所以需要 query rewrite、HyDE、query tower 训练
- 因为问题风险不同，所以需要三级动态路由和风险守卫
- 因为不能只看语言流畅性，所以需要 groundedness、coverage、人工抽检
- 因为要平衡成本，所以不是所有问题都走 Multi-Hop

### 3.2 这个项目离落地近在哪里

面试官如果怀疑是 toy，通常不是在怀疑你有没有 LangGraph，而是在看你有没有做“系统化约束”。你可以从下面几个维度讲：

- 有明确的服务边界。这个系统是医学信息辅助问答，不是自动开药、自动诊断。
- 有显式路由而不是一条链跑到底。系统区分 `No Retrieval / Single-Step / Multi-Hop`。
- 有风险守卫。高风险医学问题禁止停留在 `No Retrieval`。
- 有证据层设计。`BM25 + BGE-M3 + RRF + reranker + 少量 LLM 筛选`，不是把所有希望都压在一个 LLM 上。
- 有版本化语料治理。把服务语料、外部搜索、评测集分开管理，而不是一个 demo corpus 糊在一起。
- 有过程状态记录。`route_history`、`risk_signals`、`retrieval_round`、`screening_mode`、`evaluation` 都写进 `GraphState`，说明它是可观测的。
- 有 before/after 评测闭环。不是只说“我训了 query tower”，而是有基线对比、升级率统计、生成评测脚本。
- 有 MCP 边界。说明你在考虑外部工具接入时，不只是“能不能调 API”，还在考虑协议层抽象。

### 3.3 当前仓库里和“非 toy 化”最相关的实现位置

- 路由总入口：`graph/chains/router.py`
- 风险守卫：`graph/risk_guardrails.py`
- 查询重写与 route history 记录：`graph/nodes/rewrite_query.py`
- 检索后筛选与 gap planning：`graph/nodes/grade_documents.py`
- 最终评估与升级闭环：`graph/evaluation.py`、`graph/nodes/evaluate_generation.py`
- 状态可观测字段：`graph/state.py`
- 训练前后对比脚本：`scripts/compare_query_tower_baseline.py`、`scripts/compare_route_upgrade_baseline.py`、`scripts/compare_training_before_after.py`

### 3.4 面试里可直接背的回答

**Q：这个项目的真实场景到底是什么？**

短答：

> 这是一个医学信息辅助问答系统，不是普通百科问答，也不是固定流程 RAG。它要解决的是医学 query 脏、风险差异大、证据要求高的问题，所以系统必须动态决定是否检索、检索几轮、什么时候联网补证，以及答案是否真的可靠。

展开：

> 我后来越来越觉得，这个项目最重要的不是“模块很多”，而是场景约束很强。医学 query 经常不是规范表达，用户会用症状、缩写、口语、甚至多意图混合来提问。  
> 同时，医学问题又不是都一样危险，有些可以高层次解释，有些如果答错会有明显风险，比如药物联用、禁忌、特殊人群、急症信号。  
> 所以系统设计不能是单一路径，而必须是带风险意识的分层处理。这也是我保留 Query Router、再加风险守卫、再加多轮补证机制的原因。

**Q：为什么你说这不是 toy？**

短答：

> 因为我做的不是“检索一下再生成”的 demo，而是把场景边界、风险约束、过程观测、语料治理、评测闭环都补上了。

展开：

> 一个 toy 往往只有 happy path，但这个项目里我关注的是失败时怎么办。  
> 路由错了怎么办，要不要升级；证据不够怎么办，要不要继续本地检索还是联网；答案看起来通顺但不 grounded 怎么办；高风险问题能不能被禁止直答。  
> 这些都不是 notebook 里一条链能解决的，而是要靠状态管理、评估器、显式控制流和语料边界来支撑。

---

## 4. 路由与脏 Query：为什么系统不是按“问题长短”在分类

### 4.1 路由的本质不是分类句子，而是分配系统预算

三级动态路由的核心不是为了“看起来智能”，而是为了把不同难度、不同风险的问题分配到不同成本层级上：

- `No Retrieval` 处理低风险、稳定知识、定义解释型问题
- `Single-Step` 处理大部分单事实、单实体、单轮检索可覆盖的问题
- `Multi-Hop` 处理高风险、多实体、多条件、跨源验证、证据缺口明显的问题

一句话说，路由做的是“最低成本下的可靠回答”。

如果面试官问“凭什么分三级”，你可以直接说：

> 因为真实系统既不能所有问题都走最重链路，也不能把所有问题都交给参数知识。三级路由本质是在做可靠性、时延和成本之间的最优折中。

### 4.2 路由分类不是看长短，而是看三个维度

你最好把这句话背下来：

> 路由不是按句子长短分，而是按风险等级、推理复杂度和证据依赖度来分。

三个维度分别是什么：

- 风险等级：是否涉及诊断、药物联用、剂量、禁忌、特殊人群、急症信号
- 推理复杂度：是否需要多实体关系推理、多条件约束、比较取舍、治疗路径分析
- 证据依赖度：答案能否依靠稳定常识给出，还是必须依赖外部证据、指南或检索结果

这就是为什么有些问题虽然很短，也必须进入检索或 Multi-Hop。例如：

- “华法林和阿司匹林能一起吃吗？”
- “孕妇能不能用这个药？”
- “胸痛伴出汗要不要紧？”

它们都很短，但风险高、证据依赖强，绝不能按“短问题=直答”处理。

### 4.3 为什么 Query Router 之外还需要风险守卫层

这是一个很容易被追问的点。标准答法是：

> Query Router 解决的是复杂度和执行路径问题，风险守卫解决的是安全边界问题。这两个不是一回事。

更完整地讲：

- Router 会尽量选最低成本路径
- 但低成本路径不等于安全路径
- 医学场景里有些 query 语言形式很简单，但业务风险很高
- 所以要在 router 之后再加一个 rule-based / signal-based 的安全约束层

当前仓库里这层逻辑在 `graph/risk_guardrails.py` 和 `graph/nodes/rewrite_query.py`：

- Router 先给出 `route_strategy`
- Query rewriter 产出更规范的问题表达
- Risk guardrail 检查高风险信号
- 如果命中风险信号且原路由是 `No Retrieval`，就至少升级到 `Single-Step`

这个设计很像真实系统里的“业务规则兜底”，会让面试官觉得你有安全意识。

### 4.4 脏 Query 到底脏在哪里

医学场景的脏 query 不是只有错别字，而是包括：

- 缩写型：例如 `RA patient on MTX fever怎么办`
- 症状型：例如 `胸闷气短晚上更厉害要不要紧`
- 多意图型：例如 `糖尿病能吃什么感冒药，会不会影响血糖`
- 条件缺失型：例如 `这个指标高怎么办`
- 口语型：例如 `吃这个药会不会伤肝`
- 中英混用型：例如 `CKD 患者 metformin dose 要不要调`
- 易混淆实体型：药名相似、疾病俗称、同义词不统一

这类 query 的问题在于：用户表达的不是“检索语言”，而是“需求语言”。

所以 planning 的第一步并不是回答，而是把需求语言变成可执行语言。

### 4.5 当前仓库里脏 Query 的处理链路

当前实现实际上已经有三层处理：

第一层是规范化重写，在 `graph/chains/query_rewriter.py`：

- 把原始问题改写成更清晰、医学术语更准确的版本
- 保留疾病、症状、药物、特殊人群、时间条件等关键信息
- 可以扩展缩写
- 生成 1 到 3 条检索 query
- 决定要不要启用 HyDE

第二层是 Hybrid Retrieval：

- 稀疏侧保术语和关键词匹配
- 稠密侧保语义扩展
- `RRF` 负责把两侧结果做稳健融合

第三层是 query tower LoRA 训练：

- 不是直接改文档塔，而是先改 query tower
- 因为真实问题的脏主要发生在 query 侧
- 通过 `(query, 正文档, 易混淆负文档)` 的 triplet 做 InfoNCE，对医学易混知识边界进行拉开

### 4.6 面试里可直接背的回答

**Q：为什么不是所有问题都直接走 Multi-Hop？**

短答：

> 因为更多 agent 不等于更好。Multi-Hop 能力更强，但时延更大、成本更高，而且链路越长，噪声和错误传播也越严重，所以应该只在真正需要的时候触发。

展开：

> 在真实系统里，最优路径不是“最复杂路径”，而是“足够可靠的最低成本路径”。  
> 如果本来一个单事实问题一轮检索就能解决，却强行进入多轮补证和联网搜索，系统会变慢、变贵，还可能因为引入额外文档而把答案做得更散。  
> 所以我把 Multi-Hop 作为高复杂度和高不确定问题的兜底路线，而不是默认路线。

**Q：脏 query 为什么值得单独优化？**

短答：

> 因为很多错误不是出在生成，而是出在入口。原始 query 如果又脏又模糊，后面再强的生成模型也只能基于错误或不完整的证据回答。

展开：

> 我把脏 query 理解成整个系统的上游噪声源。  
> 如果入口没有做规范化、术语扩展、HyDE 辅助、query tower 域化训练，那么后面的召回、重排、生成其实都在为一个有偏的检索入口服务。  
> 所以 query 侧优化的 ROI 很高，这也是我优先做 query tower LoRA，而不是一开始就堆更多 agent 的原因。

---

## 5. 结果怎么判断好坏：为什么不能只看指标

这一部分是最值得你认真准备的，因为很多候选人只能说出 Recall、NDCG、RAGAS、ROUGE，但说不出“为什么这些还不够”。

### 5.1 为什么不能只看指标

原因可以分成六层。

第一，检索指标只是代理指标，不是最终目标。

- `Recall@5`、`NDCG@10`、`MRR@10` 只说明相关文档有没有被找回来、排得靠不靠前
- 它们不等于最终答案一定正确
- 真正用户看到的是 answer，不是 top-k 文档列表

第二，医学场景里“说得像对”远远不够。

- 一个答案可能语言非常流畅
- 但如果它漏了特殊人群约束、禁忌信息、不确定性表达，风险依然很高
- 这种问题很多时候 ROUGE 和 BERTScore 看不出来

第三，平均指标会掩盖尾部高风险错误。

- 即便大多数普通 query 都答得不错
- 只要少数高风险问题被错误直答、错误检索、错误总结
- 在医学场景里就是严重问题

第四，指标很难直接告诉你错在系统哪一层。

- 是 router 分错了
- 还是 query rewrite 改坏了
- 还是检索没召回
- 还是 reranker 排错了
- 还是 summary 把证据压缩坏了

单个综合分数回答不了这些问题。

第五，生成指标对“合理性”和“证据一致性”不敏感。

- ROUGE 更偏重字面重合
- BERTScore 更偏语义相似
- 但医学回答里真正关键的是有没有被证据支持、有没有遗漏风险条件、有没有表达得过于确定

第六，离线 benchmark 不等于真实场景分布。

- 标准数据集通常更干净、更规范
- 真实输入里会有缩写、模糊问法、语义缺项、口语表达
- 所以必须看抽样输出，不能只看 benchmark report

### 5.2 正确的分析方式：至少要做“五层评估”

我建议你把结果分析分成五层，这样讲会非常像真正做过系统的人。

#### 第一层：路由层

看系统有没有把问题送到合适的路径上。

重点看：

- 初始路由分布
- 最终执行路由分布
- `No Retrieval -> Single-Step` 升级率
- `Single-Step -> Multi-Hop` 升级率
- 高风险问题被错误留在 `No Retrieval` 的比例
- 各类 query 在不同 route 下的错误率

你可以强调一点：

> 我不只看 router 的初判，还会看 route history，因为系统是会升级的。真正重要的是它最后走到了哪条路，以及为什么升级。

#### 第二层：检索层

看证据有没有被找回来。

重点看：

- `Recall@5`
- `NDCG@10`
- `MRR@10`
- `top1 hit`
- 相关文档命中数
- 分 query 类型切片后的表现，比如短 query、缩写 query、多实体 query、特殊人群 query

这里最重要的不是“平均值多高”，而是：

- 哪一类 query 提升最大
- 哪一类 query 仍然很差
- 提升来自召回还是排序

#### 第三层：证据层

看检索回来的文档是不是“可用于回答”。

重点看：

- 前几条证据是否真的覆盖主问题
- 有没有明显重复或近重复文档
- 是否引入了低质量、过时或站点可信度不足的文档
- 来源是否足够多样但不过度发散
- reranker 之后前几条是否更聚焦

这层非常容易被忽视，但它很关键，因为“召回到”不代表“可用”。

#### 第四层：生成层

看最终答案是否既回答了问题，又被证据支持。

重点看：

- `addresses_question`
- `faithfulness`
- ROUGE-L
- BERTScore
- 是否覆盖了问题的关键 facet
- 是否表达了不确定性和边界

当前仓库中，这层评估已经拆成两件事：

- `answer_grader` 看“有没有回答到点上”
- `hallucination_grader` 或 `RAGAS Faithfulness` 看“是不是 grounded”

这两个维度拆开非常重要，因为“回答了问题”和“有证据支撑”不是一回事。

#### 第五层：安全与业务层

这是最像真实落地系统的一层，也是很多人面试答不出来的一层。

重点看：

- 高风险问题是否至少进入检索
- 是否出现过 unsupported 的药物剂量、禁忌、诊疗判断
- 是否把不确定问题回答成了确定结论
- 是否漏掉了特殊人群限制
- 是否出现“检索到了，但 summary 说满了”的情况

如果你把这层讲出来，面试官通常会觉得你的视角已经从“模型效果”转向“系统可靠性”了。

### 5.3 结果分析不该停留在分数，而要落到 case review

真正有价值的分析方式是“样本级复盘”，建议你背这套顺序：

1. 先看原始问题，是哪一类 query
2. 再看重写后的 query，有没有丢信息或错误扩展
3. 再看路由结果是否合理，风险信号有没有命中
4. 再看 top-k 检索文档和 rerank 后排序
5. 再看最终用于生成的 filtered documents
6. 再看最终答案有没有覆盖主问题
7. 再看答案中的关键 claim 是否都有证据支持
8. 最后给这条失败样本打 root cause 标签

我建议 root cause 至少分成这几类：

- `route_error`
- `rewrite_error`
- `retrieval_miss`
- `rerank_error`
- `evidence_insufficient_but_answered`
- `summary_omission`
- `summary_overconfidence`
- `source_quality_issue`

这样你在面试时就能说：

> 我不是只看总分，而是会把错误按系统层级打标签，因为只有这样才能知道下一步该优化 router、query tower、reranker、summary prompt 还是语料。

### 5.4 用更强模型分析答案准确性和合理性，应该怎么做

这部分非常适合当作“高级回答”，因为它说明你对评估并不满足于一个二分类 grader。

核心思想是：

> 不要让一个更强的模型只给一个“好/不好”的总分，而是让它做结构化、分步骤、可回溯的 judge。

推荐把 judge 设计成四步。

#### 第一步：问题分解

先让 judge 模型把用户问题拆成必须回答的 facet。

例如一个问题是：

> “CKD 患者吃 metformin 需要调整剂量吗，会不会增加乳酸酸中毒风险？”

那 judge 应该至少提取出两个 facet：

- 是否需要剂量调整
- 风险点是什么，是否提到了乳酸酸中毒

这样后面才能判断 summary 是否“覆盖完整”。

#### 第二步：答案 claim 抽取

再让 judge 模型把最终答案拆成原子化 claim。

例如：

- “CKD 患者使用 metformin 需要根据肾功能评估”
- “重度肾功能不全时风险增加”
- “与乳酸酸中毒存在关联”

这样做的意义是把“整段答案好不好”变成“每个 claim 有没有依据”。

#### 第三步：claim-evidence 对齐

对于每个 claim，让 judge 模型输出：

- `supported`
- `partially_supported`
- `not_supported`
- `contradicted`

并且要求它返回支持该 claim 的文档编号或证据片段。

这样你就能把 groundedness 从一个整体布尔值，升级成 claim-level 的精细评估。

#### 第四步：综合判定

最后再汇总成几个维度：

- `coverage_score`：问题 facet 覆盖是否完整
- `grounding_score`：claim 支持率如何
- `calibration_score`：是否把不确定性表达正确
- `safety_flag`：是否出现高风险 unsupported claim
- `final_verdict`：可接受 / 需升级 / 不可接受

这比直接问一个模型“这个答案好不好”有三个明显好处：

- 可解释。你知道坏在哪一个 claim、哪一个 facet
- 可定位。你能区分是证据不足还是 summary 说过头了
- 可迭代。你知道后面该优化哪一层

#### 更强 judge 的工程建议

如果要落到实现，建议不要让生成模型和 judge 模型完全共用一套配置，而是显式拆开：

- 生成模型负责回答
- judge 模型负责评估
- judge 温度设为 0
- judge 使用结构化输出
- judge 提示词只允许基于问题和证据判定，不允许自由发挥

当前仓库已经有一个比较轻量的版本：

- `graph/chains/answer_grader.py` 负责 coverage 判断
- `graph/chains/hallucination_grader.py` 负责 groundedness 判断
- `graph/evaluation.py` 优先尝试 `RAGAS Faithfulness`，失败时回退到 LLM judge

如果以后要继续升级，可以在 `model.py` 单独增加 `evaluator_model`，把 judge 逻辑从 `grader_model` 再拆开，做成更强、独立、更稳定的评估器。

### 5.5 面试里可直接背的回答

**Q：为什么你说不能只看指标？**

短答：

> 因为指标只能告诉我“平均上变好了没有”，但医学场景里更重要的是“有没有高风险错误”“答案是不是被证据支持”“问题有没有真的被回答到”。这些很多都不是单个指标能反映出来的。

展开：

> 检索指标本质上是代理指标，它只能说明相关文档有没有被找回来、排得靠不靠前，但用户最终看到的是答案。  
> 医学场景又特别怕两类问题：第一类是证据不足但说得很像对，第二类是漏掉关键限制条件但整体看起来还不错。  
> 所以我现在做结果分析时，至少会分成路由、检索、证据、生成、安全五层去看，并且会抽样做 case review，而不是只盯着 Recall 或 ROUGE。

**Q：那你怎么判断一个答案到底好不好？**

短答：

> 我会同时看三件事：有没有回答到问题、有没有证据支持、有没有把风险和不确定性表达对。

展开：

> 我现在不会只看一句“这个答案像不像正确答案”，而是会先把问题拆成几个必须覆盖的 facet，再看最终 summary 有没有都覆盖；  
> 然后再看答案里的关键 claim 有没有对应证据支持；  
> 最后再看它有没有过度确定、有没有漏特殊人群限制、有没有把证据不足的地方说清楚。  
> 如果要更细，我会让更强的 judge 模型做 claim-level 审核，而不是只给一个总分。

---

## 6. 实施前的 Planning：它不是“提前想一想”，而是把问题变成可执行图

这一部分你一定要理解透，因为很多面试官问 planning，并不是在问“你有没有写 plan.md”，而是在问：

> 这个系统在真正执行前，是否知道自己要查什么、走哪条路、缺什么证据、下一步应该做什么。

### 6.1 在 Agent 系统里，planning 的本质是什么

你可以先背这句总定义：

> 在 agent 系统里，planning 的本质不是写一段自然语言计划，而是把用户问题映射成一条可执行、可约束、可回退的行动路径。

如果更工程化一点，可以这么说：

> planning 决定系统“看什么、先看什么、不够时再看什么”，它的作用是降低搜索空间、减少无效调用、避免错误路径早早固化。

### 6.2 这个项目里其实有三层 planning

这是一个很值得讲的点，因为它会让你显得对系统理解非常细。

#### 第一层：宏观 planning，也就是 route planning

问题是：这道题大概要走哪条成本层级的路线？

对应实现：

- `graph/chains/router.py`
- `graph/nodes/rewrite_query.py`

输入是原始问题，输出是：

- `no_retrieval`
- `single_step`
- `multi_hop`

它解决的是“先不先检索、检索到什么程度”的问题。

#### 第二层：检索 planning，也就是 retrieval planning

问题是：如果要检索，应该拿什么 query 去查？

对应实现：

- `graph/chains/query_rewriter.py`

它输出的不是一个“文案改写结果”，而是一份可执行的检索计划：

- `rewritten_question`
- `retrieval_queries`
- `use_hyde`

这一步非常关键，因为原始用户问题往往不是检索友好的表达。

#### 第三层：证据缺口 planning，也就是 gap planning

问题是：查完一轮以后，如果证据还不够，下一步应该补什么？

对应实现：

- `graph/chains/gap_analyzer.py`
- `graph/nodes/grade_documents.py`

它会输出：

- `next_action`
- `missing_information`
- `sub_queries`
- `search_query`

这一步不是单纯说“再查一下”，而是显式分析：到底是继续本地检索，还是应该联网，还是其实已经足够可以生成。

### 6.3 为什么 planning 在这个项目里尤其重要

因为这个项目上游有两个天然问题：

第一，原始 query 不等于检索 query。

- 用户说的是需求语言
- 系统要执行的是检索语言

第二，医学问题很容易有“隐含子问题”。

例如：

> “CKD 患者吃 metformin 要注意什么？”

它背后可能隐含：

- 剂量调整
- 禁忌
- 乳酸酸中毒风险
- 肾功能分层

如果 planning 没有把这些要素识别出来，后面的 retrieval 和 summary 就很容易只答到一半。

第三，多轮检索如果没有 planning，很容易变成盲目重试。

- 不知道为什么继续查
- 不知道该查哪个子问题
- 不知道何时停止
- 不知道何时联网

一旦这样，系统就会显得“很忙，但不聪明”。

### 6.4 当前仓库中的 planning 实现细节

#### Query Rewriter 是“检索规划器”

`graph/chains/query_rewriter.py` 里，LLM 被定义成 `retrieval planning module`，说明它的职责不是改写语句本身，而是为检索服务。

它做了三件事：

- 规范问题表达
- 产出 1 到 3 条检索 query
- 判断是否启用 HyDE

这里最值得在面试里强调的一点是：

> planning 的产物不是一句漂亮的话，而是一组能被下游模块消费的结构化字段。

#### Gap Analyzer 是“证据缺口规划器”

`graph/chains/gap_analyzer.py` 里，LLM 被定义成 `evidence-gap planner`。

它不是在评价答案，而是在评价“当前证据状态是否足够支持下一步动作”。

它的价值在于：

- 防止 Multi-Hop 变成无限检索
- 防止系统一证据不足就盲目联网
- 让多轮补证是有目标地进行

#### GraphState 让 planning 可持续

`graph/state.py` 里保留了：

- `route_history`
- `retrieval_queries`
- `sub_queries`
- `retrieval_round`
- `retry_count`
- `evaluation`

这意味着 planning 不是一锤子买卖，而是可在执行过程中不断修正的。

### 6.5 planning 失败时会出现什么问题

这是你可以用来体现“看过系统失败样本”的地方。

如果 planning 不好，常见错误有：

- 改写把关键信息改丢了
- 扩写引入了错误医学含义
- 本该分解子问题却只生成一个宽泛 query
- 本该联网验证却继续在本地语料里兜圈子
- 本该升级到 Multi-Hop 却停在 Single-Step
- 高风险问题没有被显式识别

这些问题的共同点是：

> 它们不是 summary 阶段能完全补救的，因为 planning 错了，后面拿到的证据就已经偏了。

### 6.6 面试里可直接背的回答

**Q：你怎么理解实施前的 planning？**

短答：

> 我理解的 planning 不是写一个自然语言计划，而是把用户问题变成可执行路径。它决定系统走哪条 route、用什么 query 检索、证据不足时下一步补什么。

展开：

> 在这个项目里，planning 至少有三层：第一层是 route planning，决定是 `No Retrieval`、`Single-Step` 还是 `Multi-Hop`；第二层是 retrieval planning，把用户的脏 query 变成结构化检索 query；第三层是 evidence-gap planning，在多轮检索里判断到底是继续本地查、联网查，还是已经足够生成。  
> 所以 planning 不是一个附属功能，而是整个 agent workflow 的前半个“大脑”。它决定系统看什么、先看什么、缺什么还要继续看。

**Q：为什么 planning 在这个项目里这么重要？**

短答：

> 因为用户问题不是天然可执行的，而医学问题又经常是多条件、多约束的。如果没有 planning，后面的检索和生成都会建立在一个模糊入口上。

展开：

> 我后来比较深的体会是，很多失败不是模型不会答，而是系统一开始就查错了方向。  
> planning 的价值就是在最前面把搜索空间缩小，把关键实体、隐含子问题、风险条件先抽出来。  
> 这样后面的 retrieval、rerank、summary 才有可能建立在正确的证据基础上。

---

## 7. 最终 Summary：它不是“把文档压缩一下”，而是最后一层风险控制

这部分你如果能讲深，面试官通常会觉得你真的理解 RAG 的终点不是 retrieval，而是“负责任地表达检索到的东西”。

### 7.1 summary 的本质是什么

你可以先背这句：

> planning 决定系统看什么，summary 决定系统说什么。

如果再深入一点：

> final summary 不是简单摘要，而是把多源证据压缩成一个对用户有用、对证据忠实、对不确定性诚实的最终回答。

它本质上是一个“受约束的信息压缩问题”。

为什么这么说：

- 检索回来的是一堆片段，不是最终答案
- 用户要的不是文档列表，而是面向问题的结论
- 但 summary 又不能超出证据范围自由发挥

所以好的 summary 必须同时满足：

- 回答问题
- 尊重证据
- 控制不确定性
- 保留关键限制条件

### 7.2 这个项目里的 final summary 是怎么做的

当前仓库里 summary 的主要实现位置是：

- `graph/chains/generation.py`
- `graph/nodes/generate.py`

`generation.py` 里其实已经把 final summary 的行为定义得比较清楚了：

- 必须优先使用 context
- 必须综合多份文档，而不是照抄一份
- 如果证据不足、冲突或缺失，要明确说出来
- 禁止编造 citation、URL、药物剂量、禁忌等高风险信息
- 不暴露内部链路
- 不同 route 有不同总结风格

它还规定了一个很重要的输出结构：

- 先直接回答
- 再给一个简短的“依据”部分
- 如果仍有不确定性，再给“局限/注意”部分

这其实已经不是普通意义上的聊天输出，而是更接近面向医学知识问答的 evidence-grounded response。

### 7.3 一个好的 final summary 应该具备什么特征

建议你用下面五条来分析。

第一，正确性。

- 内容本身不能和证据冲突
- 不能引入证据里没有的高风险判断

第二，完整性。

- 要回答用户真正问的问题
- 多部分问题不能只答一半

第三，groundedness。

- 核心 claim 要有证据支撑
- 不能靠语言流畅性“补脑”

第四，校准性。

- 有把握时说清楚
- 没把握时要承认局限
- 证据冲突时要表达冲突，而不是伪装成单一结论

第五，边界意识。

- 不把信息辅助写成个体化诊疗建议
- 高风险问题不乱给确定性结论

### 7.4 为什么 summary 也是系统容易出错的一层

很多面试官会默认问题都在 retrieval，其实不是。summary 很容易出现四类错误：

#### 1. 证据在，答案没说全

例如检索文档里已经有特殊人群限制，但 summary 只给了一般结论，没有把限制条件带出来。

#### 2. 证据不够，答案说满了

这在医学场景最危险。尤其是治疗、剂量、风险判断，如果证据只是一般性说明，但 summary 写成了确定建议，就会产生很大风险。

#### 3. 多源证据被“抹平”

多篇文档存在细微差异或冲突时，summary 容易强行压成一个统一答案，导致不确定性被隐藏。

#### 4. summary 被最靠前的一篇文档绑架

即使系统有多篇证据，生成模型也可能被第一篇最强片段带偏，忽略其他文档中的关键 caveat。

### 7.5 如何分析一个 final summary 好不好

这是非常重要的一套方法论，你可以原样讲。

不要直接问“这个 summary 好不好”，而是按下面顺序分析：

1. 用户问题有几个关键 facet
2. summary 是否覆盖了所有 facet
3. summary 中有哪些关键 claim
4. 每个 claim 是否都能在 evidence 中找到支持
5. 是否遗漏了适用条件、禁忌、不确定性
6. 是否把“可能/一般原则”说成了“确定建议”
7. 如果 evidence 有冲突，summary 有没有体现

如果你按这个框架分析，面试官会明显感觉到你是在做“临床信息表达控制”，而不是在看一段文案顺不顺。

### 7.6 如何用更强模型评估 final summary

这里可以把第 5 节的 judge 设计进一步落到 summary 上。

推荐的高阶做法是：

#### Step 1：question facet extraction

把用户问题拆成必须覆盖的 facet。

#### Step 2：summary claim extraction

把答案拆成原子 claim。

#### Step 3：evidence attribution

让 judge 为每个 claim 找支持证据文档编号。

#### Step 4：support verdict

输出：

- `supported`
- `partial`
- `unsupported`
- `contradicted`

#### Step 5：reasonableness and calibration check

除了“对不对”，还要看“说得合不合理”：

- 证据不足时有没有保守表达
- 风险高时有没有显式提醒边界
- 冲突证据时有没有错误地给出单一确定结论

也就是说，更强模型不是只帮你验真假，还可以帮你验“表达方式是否合理”。

### 7.7 当前仓库与更高级 summary 评估的关系

当前仓库已经有一个基础版闭环：

- `graph/chains/answer_grader.py` 做 coverage 判断
- `graph/chains/hallucination_grader.py` 做 groundedness 判断
- `graph/evaluation.py` 优先用 `RAGAS Faithfulness`
- `graph/nodes/evaluate_generation.py` 在回答不充分或不 grounded 时触发升级或重试

这已经说明系统不是“生成完就结束”，而是把 summary 也纳入了控制流。

如果后续要继续增强，最合理的方向是：

- 把二分类 grader 升级为 claim-level structured judge
- 为高风险问题增加单独的 safety rubric
- 把 summary 的错误拆分成 coverage、grounding、calibration 三类

### 7.8 面试里可直接背的回答

**Q：你怎么理解最终的 summary？**

短答：

> final summary 不是把检索结果压缩一下，而是把多源证据组织成一个既回答问题、又不超出证据边界的最终表达。它其实是系统最后一层风险控制。

展开：

> 检索和重排只是帮系统找到信息，真正面向用户的是最后的 summary。  
> 在医学场景里，summary 如果只追求流畅，会很容易把证据不足的问题说满，或者把限制条件遗漏掉。  
> 所以我把 final summary 理解成受约束的证据整合：它既要覆盖问题，又要对证据忠实，还要把不确定性表达正确。  
> 当前仓库里我已经把答案生成、coverage 评估、groundedness 评估和失败升级接起来了，本质上就是让 summary 也成为可控环节，而不是自由生成。

**Q：为什么 summary 不能简单理解成“总结文档内容”？**

短答：

> 因为用户要的是“对问题的回答”，不是“对文档的摘要”。summary 必须围绕问题组织证据，而不是机械拼接检索片段。

展开：

> 同样一组文档，对不同问题应该得到不同的 summary，因为 summary 的目标不是压缩信息量，而是把与当前问题相关的证据重新组合。  
> 如果只是做文档摘要，很容易出现两个问题：第一是答非所问，第二是把文档里的 caveat 和适用条件丢掉。  
> 所以我会把 summary 看成 question-conditioned evidence synthesis，而不是 generic summarization。

---

## 8. 一套你可以直接拿去讲的“结果分析方法论”

如果面试官问你：

> “那你平时到底怎么分析结果？”

建议你就按下面这段答：

> 我不会只看几个总指标，而是把结果分析拆成三步。  
> 第一步看分层指标，包括路由、检索、生成和安全指标，先判断系统是哪里变了。  
> 第二步看切片分析，比如短 query、缩写 query、多实体 query、高风险 query、需要联网的 query 分别表现怎样。  
> 第三步做样本级复盘，从原始 query、rewrite、route、top-k 文档、rerank、filtered evidence 一直看到 final summary，把错误定位到具体层。  
> 因为只有这样我才能回答“为什么变好”“变差发生在哪”“下一步应该优化什么”，而不是只会报一个平均分。

如果面试官继续追问“切片分析怎么做”，你可以补：

- 按 query 长度切
- 按 query 是否缩写切
- 按风险等级切
- 按初始 route 切
- 按最终 route 切
- 按是否启用 HyDE 切
- 按是否触发 websearch 切
- 按问题类型切，比如定义类、药物联用类、特殊人群类、方案比较类

这样你会显得非常像真实做过系统评估的人。

---

## 9. 面试里最容易加分的几句话

下面几句建议你熟记，几乎可以插入任何相关问题里。

1. `路由不是按句子长短分，而是按风险等级、推理复杂度和证据依赖度来分。`
2. `planning 决定系统看什么，summary 决定系统说什么。`
3. `医学场景最怕的不是答不出来，而是答得很像对的，但证据不够。`
4. `我不会只看平均指标，还会看高风险样本、错误切片和 case review。`
5. `很多问题不是生成模型不会答，而是系统一开始就查偏了，所以 planning 和 query 侧优化的 ROI 很高。`
6. `一个答案是否好，不只是看语义像不像参考答案，还要看有没有回答到点、有没有证据支持、有没有把不确定性表达对。`

---

## 10. 你可以怎么把这份文档真正用起来

建议练三轮：

第一轮，只练“短答”。

- 每个问题先控制在 20 到 30 秒
- 重点让自己先讲出结论

第二轮，练“展开”。

- 每个问题讲到 1 分钟左右
- 重点把场景约束和系统设计串起来

第三轮，练“反追问”。

- 每答完一句，自己追问一句“为什么”“怎么判断”“失败了怎么办”
- 逼自己落到 planning、summary、case review、root cause 分析

如果你能把这一轮练熟，面试官通常会感觉你真的理解了这个项目的系统性，而不是只背了仓库结构。
